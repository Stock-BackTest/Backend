<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockbacktest.backend.search.mapper.ProductMapper">

  <select id="searchProductByKeyword" resultType="string">
    SELECT DISTINCT ISIN_CD
    FROM SECURITIES_PRODUCTS_INFO
    WHERE 1=1
    AND (
    SECURITY_NAME ILIKE ('%' || #{keyword} || '%')
    OR ISIN_CD ILIKE ('%' || #{keyword} || '%')
    OR ISIN_SHRT_CD ILIKE ('%' || #{keyword} || '%')
    )
  </select>
</mapper>